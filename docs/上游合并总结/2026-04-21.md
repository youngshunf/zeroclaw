# 上游合并总结 — 2026-04-21

## 基本信息

| 项 | 值 |
|---|---|
| 合并日期 | 2026-04-21 |
| 目标分支 | `upstream/master`（`zeroclaw-labs/zeroclaw`） |
| 上一同步点 | `021d6664`（2026-04-20） |
| 上游版本 | 0.7.3（无版本跳） |
| 上游提交数 | 21 个 |
| 文件改动 | 46 个（+2404 / -654） |
| 冲突文件 | 2 个（`Cargo.lock` 机械冲突 + `src/main.rs` logging 初始化区） |
| 合并 commit | `ab9e7103` |

---

## 一、Agent / LLM 核心

| PR | 改动 | 对唤星影响 |
|---|---|---|
| **#5828** | `prune_history` Phase 1：mixed-protection tool 组当作原子单元修剪 | 🟡 多租户 agent 历史修剪行为变化，需观察是否影响 HASN session |
| **#5853** | runtime 自愈孤儿 `tool_result` 块（load + compact 时） | 🟢 多 agent 场景受益 |
| #5827 | 没有 delivery handler 时 `deliver_announcement` 返回 Err（不静默吞错） | 🟢 |
| #5821 | providers `flatten_system_messages` 测试加 `merge=true` | 🟢 测试加固 |

---

## 二、Provider

| PR | 改动 |
|---|---|
| **#5623** | OpenRouter 新增 `extra_body` 透传通用请求参数 |
| #5830 | OpenRouter 流任务在 consumer drop 时 abort |
| #5848 | Groq provider 禁用 native tools（与飞书 mention_only 一并） |

---

## 三、渠道

| PR | 改动 |
|---|---|
| **#5848** | 飞书 `mention_only` 配置接通（仅 mention 时回复） |
| #5641 | Web：WebSocket session ID `localStorage` 跨页面刷新持久化 |
| 5c81d4e4 | ACP 渠道：抑制 INFO 日志 + 实现缺失的 ACP spec 协议 |

---

## 四、配置 / 平台

| PR | 改动 | 备注 |
|---|---|---|
| **#5950** | `Vec<String>` 字段暴露给 `zeroclaw config get/set/list` | 🟢 唤星 config CLI 可读写数组项 |
| **#6041** | hardware wizard UI 从 `src/main.rs` 抽到 `zeroclaw_hardware::wizard` crate | 🟢 减小 main.rs 体积 |
| #5904 | sandbox auto-detection 尊重 `runtime.kind = "native"`（修 #5719） | 🟢 |
| 321e96ff | config：provider 条目 mirror 到 canonical fallback key | 🟢 配置兼容性 |

---

## 五、Skills / 工具链

| PR | 改动 |
|---|---|
| **#6045** | registry-based bare-name skill install（`/skill install foo` 自动从 registry 取） |
| #5973 | `pr-review-session` skill 中性化措辞，自动从 `gh auth` 检测 reviewer |

---

## 六、CI / 安全

| PR | 改动 |
|---|---|
| **#6011** | `rustls-webpki` 升级 v0.103.13 + 忽略 v0.102.8（不可修复副本） |
| **#5971** | rand patch 升级 + picomatch ReDoS 修复 + wasmtime/glib 忽略文档化 |
| #5926 | `ci-run.yml` 整合 + CI 文档重写 |
| #5996 | release Docker 镜像包含 web dashboard |
| #5968 | 安装脚本：`curl \| bash` 默认走 pre-built（提示询问 source vs pre-built） |
| #6050 | Windows：修 `cargo test` + 增 self-update 目标三元组 |

---

## 七、文档 / 其他

- 21 commits 中无大型重构（onboard wizard 维持 0.7.3 状态）。
- 上游未触碰 HASN / 唤星 fork 的多租户路径。

---

## 对唤星 fork 的影响评估

| 项 | 评估 |
|---|---|
| 冲突面 | 🟢 仅 2 个机械冲突（Cargo.lock + main.rs logging 初始化区） |
| 签名兼容 | 🟢 trait 注入接口零变化 |
| HASN 路径 | 🟢 完全未触 |
| 桌面端 | 🟢 `clients/desktop/` 完全独立，零改动 |
| 编译验证 | ✅ `cargo check --bin zeroclaw`（无 cfg 泄漏）✅ `--features huanxing` |

### 唯一重要冲突 — `src/main.rs` logging 初始化

- **上游意图**（5c81d4e4 / 类似 PR）：ACP 模式下 stdio 被 JSON 协议占用，日志默认改 `warn` + 重定向到 `stderr`。
- **唤星 HEAD**：完整的 file appender + 多 layer registry（桌面端 sidecar 日志归档刚需）。
- **本次决策**：**保留 HEAD**，在源码内嵌 `TODO` 注释标注待办，另起 commit 适配（让 ACP 命令时 stdout_layer 改 stderr 即可，file_layer 保持不变）。

---

## 待跟进事项

- [ ] **ACP stdio 污染保护适配**（src/main.rs L1004）：在 ACP 命令时让 stdout_layer 改用 stderr，file_layer 不变。预计 5 行改动。
- [ ] 评估 `#5828 mixed-protection tool 原子修剪` 对 HASN session 长程历史的影响。
- [ ] 验证 `#6045 registry-based skill install` 与唤星 `skill_market_tools.rs` 是否冲突。
- [ ] 冒烟测试：sidecar 启动 + 桌面端 HASN。

---

## 合并命令回放

```bash
git fetch upstream
git merge upstream/master --no-commit --no-ff
# Cargo.lock：git checkout --theirs Cargo.lock
# src/main.rs L1004-1075：保留 HEAD（file appender），加 TODO 注释标 ACP 适配
cargo check --bin zeroclaw                      # 重算 lock + 验无 cfg 泄漏
cargo check --bin zeroclaw --features huanxing  # 验主路径
git add Cargo.lock src/main.rs
git commit -m "chore(upstream): 合并 zeroclaw-labs/zeroclaw（21 commits / 46 files）"
```
