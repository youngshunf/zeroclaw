# 上游合并总结 — 2026-04-20

## 基本信息

| 项 | 值 |
|---|---|
| 合并日期 | 2026-04-20 |
| 目标分支 | `upstream/master`（`zeroclaw-labs/zeroclaw`） |
| 上一同步点 | `160a1c50`（约 2026-03-xx） |
| 上游版本 | **0.6.9 → 0.7.3**（经 0.7.0、0.7.1 三连跳） |
| 上游提交数 | 61 个 |
| 文件改动 | 167 个（+12409 / -4116） |
| 冲突文件 | 仅 2 个（`Cargo.toml` + `Cargo.lock`，全为机械冲突） |
| 合并 commit | `c32c5ace` |

---

## 一、Agent / LLM 核心

| PR | 改动 | 对唤星影响 |
|---|---|---|
| **#5168** | **HMAC 工具执行凭证** — 服务端签发凭证防 LLM 幻觉工具调用 | 🟡 需验证 `TenantContext::create_agent` 仍可通过 agent loop；多租户 agent 也享受该防护 |
| #5817 | cron 调度防内存雪崩（agent job 累积） | 🟢 唤星多 agent 场景直接受益 |
| #5774 | agent gateway 路径注册 skill tools + excluded filter | 🟢 skill 市场工具链路更稳 |
| #5565 | 空成功工具输出规范化 | 🟢 工具返回统一 |
| #5864 | multimodal image-marker 解析加固（非路径载荷） | 🟢 图片消息更健壮 |
| #5857 | embedding score 乘 100 百分比修正 | 🟢 RAG 显示修复 |
| #5773 | shell 工具全开放时跳过 expansion guard | 🟡 权限边界变化，唤星权限层需复核 |

---

## 二、Provider / 提供方兼容层

> 🔴 `crates/zeroclaw-providers/src/compatible.rs` ±493 行重写，最大单文件改动。

| PR | 改动 |
|---|---|
| #5762 / #5806 | `tool_stream` 只对 Z.AI provider 发送（其他 provider 流式路径剥离） |
| #5717 | OpenRouter provider 流式支持 |
| —— | 原生工具消息在 `native_tool_calling=false` provider 里剥离 |

---

## 三、渠道

| PR | 改动 |
|---|---|
| **#5712** | **IMAP 轮询回退** — 不支持 IDLE 的邮件服务器也能收信 |
| #5727 | Matrix 加密媒体下载 + 出站附件支持 |
| #5790 | Telegram 工具审批请求用 `inline_keyboard` |
| #5802 | CLI 交互模式注册 channel factory |
| #5799 | daemon + onboard 把 webhook 纳入 channel supervisor |
| #5663 | 新增《LINE Messaging API 接入指南》 |
| #4917 | DM / 群组 mention 文档 |

---

## 四、插件 / 平台

| PR | 改动 | 备注 |
|---|---|---|
| **#5913** | **WASM 插件桥接 Phase 2 D2** — Extism 执行桥接落地 | 🟡 `crates/zeroclaw-plugins` +7 文件，新增 `runtime.rs`，唤星如要暴露工具插件需跟 |
| #5880 | OpenShift / K8s 部署清单 | 🟢 生产部署参考 |
| #5552 | 本地 AI provider 用容器感知 URL（onboard） | 🟢 Docker 场景修复 |
| #5931 | `aarch64-unknown-linux-gnu` 交叉编译链接器配置（RPi） | 🟢 边缘设备预留 |
| #5321 | GitHub Copilot onboarding 接入 | 🟡 唤星 onboard 已深度定制，不受影响 |
| #5495 | `Dockerfile.ci` 默认 CMD 改为 daemon | 🟢 |
| #5563 | Windows 隐藏 shell 控制台窗口 | 🟢 |

---

## 五、配置 / Onboard

> 🔴 `crates/zeroclaw-runtime/src/onboard/wizard.rs` **+824 行**，`crates/zeroclaw-config/src/schema.rs` -278 行（字段迁到 `scattered_types.rs`）。

- onboard 向导整体重写 — 唤星桌面端通常跳过 CLI 向导，理论无影响；但如果 `HuanXingConfig` 引用了原 `schema.rs` 的字段，后续出问题时应查 `scattered_types.rs`。
- **#5700** `otel_headers` 配置项 — 认证的 OTLP exporter 可直接带 header（唤星可观测性可用）。
- #5767 theme mode 选择器视觉预览色块（桌面端 web）。
- #5724 theme mode light/dark 切换修复。

---

## 六、Memory

| PR | 改动 |
|---|---|
| #5631 | 新增 `is_user_autosave_key` 检测 per-turn 用户消息键 |
| #5632 | 所有 memory context 路径跳过 user autosave keys |

🟢 唤星多租户 memory 受益，避免用户消息被误作为长期记忆保留。

---

## 七、可观测性

- `observability-prometheus` feature 现**同时转发到 `zeroclaw-gateway`** —— 网关侧 Prometheus 指标生效（唤星 Cargo.toml 已采上游此改动）。

---

## 八、Skills / 工具链（`.claude/skills/`）

| Skill | 状态 | 用途 |
|---|---|---|
| `changelog-generation/` | 新增（#5819） | 用 GraphQL 自动更新 CHANGELOG-next.md 贡献者 |
| `github-issue-triage/` | 新增（#5780） | Issue 分诊协议 |
| `github-pr-review-session/` | 新增（#5910） | 取代 `github-pr-review`，落地 ZeroClaw Maturity Framework |
| `github-pr-review/` | **已删除** | 由上方新 skill 顶替 |
| `squash-merge/` | 新增（#5782） | **为上游合并保留 commit 历史**（下次合并可直接用） |

👉 **建议**：下次上游合并先阅读 `.claude/skills/squash-merge/SKILL.md`，按其协议走。

---

## 九、CI / 安全

| PR | 改动 |
|---|---|
| **#5786** | 升级 `rustls-webpki` / `rumqttc` 修 RUSTSEC-2026-0098 / 0099 |
| #5925 | pin 所有 action refs（供应链） |
| #5922 | 移除 5 个危险 / 冗余 workflow |
| #5928 | 每日 advisory scan workflow |
| #5924 | Dockerfile COPY 脆性修复 |
| #5920 | 新增 maintainer manual release runbook |
| #5929 | 移除 crates-io publish job（发布收敛） |
| #5914 | CI Required Gate 移除 docs-quality |
| #5894 | release workflow `RELEASE_TOKEN → GITHUB_TOKEN` |

---

## 十、版本 / 发布

- `0.7.0 → 0.7.1 → 0.7.3` 三连跳（`0.7.2` 因 tag 不可变性问题跳过，见 #5860）。
- `workspace.package.version` 已升 `0.7.3`，workspace 内部依赖版本号同步。
- `publish = false` 保留 — 依赖 RFC #5579 发布拓扑，短期不发 crates.io。

---

## 十一、文档 / 基金会

- **ZeroClaw Maturity Framework** 正式落地（#5911，`docs/foundations/`）。
- #5648 PR 模板从 15 section 精简到 7。
- #5791 CONTRIBUTING 加 "When to Supersede" 与 fixup 归属规则。

---

## 对唤星 fork 的影响评估

| 项 | 评估 |
|---|---|
| 冲突面 | 🟢 极小 — 抽象层契约（trait 注入 + 单行 `.merge()`）扛住 61 个 commit |
| 签名兼容 | 🟢 `register_huanxing_tools_fn` / `register_huanxing_channels_fn` / `TenantContext::create_agent` 签名未变 |
| HASN 路径 | 🟢 上游未触碰 HASN，`hasn-node` / `hasn-client-core` / `src/huanxing/hasn_*.rs` 零影响 |
| 桌面端 | 🟢 `clients/desktop/` 完全隔离，零改动 |
| 编译验证 | ✅ `cargo check --bin zeroclaw`（纯上游，无 cfg 泄漏）✅ `--features huanxing` ✅ clippy 无错误 |
| Clippy 警告 | 🟡 83 个 `collapsible_if`（Rust 1.92 新 lint 触发**预存** `crates/zeroclaw-huanxing/` 代码，非合并引入）+ 1 个 `semicolon_if_nothing_returned` |

---

## 待跟进事项

- [ ] 冒烟测试（Step 4）：`cargo test --features huanxing`；sidecar 启动 `./target/debug/zeroclaw gateway -p 42620`；桌面端 `pnpm tauri dev` 验证 HASN 链路。
- [ ] Clippy 警告批量清理（另起 `chore(clippy):` commit，`cargo clippy --fix`）。
- [ ] 评估是否采用 `#5168 HMAC 工具执行凭证`（需确认对唤星 task-local security 模型的兼容性）。
- [ ] 下次合并前阅读并采用 `.claude/skills/squash-merge/SKILL.md`。

---

## 合并命令回放

```bash
git fetch upstream
git merge upstream/master --no-commit --no-ff
# 解决 Cargo.toml（workspace deps 版本号 + observability-prometheus feature）
git checkout --theirs Cargo.lock
cargo check                        # 让 cargo 重算 huanxing 路径依赖
git add Cargo.toml Cargo.lock
git commit -m "chore(upstream): 合并 ..."
```
